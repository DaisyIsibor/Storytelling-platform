{{!-- Placeholders for a specific story and user who wrote it --}}

<div class="story">
    <h2>{{ story.title }}</h2>
    <p>{{ story.content }}</p>
    <p>Author: {{ story.author }}</p>
    <p>Theme: {{ story.theme }}</p>
    <p>Published on: {{ formatDate story.date }}</p>
    
</div>
