<div class="card">
        <h2 class="card-header"> Edit Post Here</h2>
        <form class="card-body edit-form">
            <div class="form-group">
                <label class="form-label">Story Title: </label>
                <input type="text" class="form-input" name="story-title" value="{{story.title}}"/>
            </div>
            <div class="form-group">
                <label class="form-label">Content</label>
                <textarea class="form-textarea form-input" name="story-content">{{story.content}}</textarea>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary">Edit Post</button>
            </div>
        </form> 
</div>